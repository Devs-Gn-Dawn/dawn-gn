doctrine:
  orm:
    dql:
    mappings:
      App:
        is_bundle: false
        type: annotation
        dir: "%kernel.project_dir%/src/Entity"
        prefix: 'App\Entity'
        alias: App
        debug: true # Ajoutez cette ligne pour activer les logs de débogage
