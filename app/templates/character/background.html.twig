{% extends 'base.html.twig' %}

{% block title %}Background du Personnage{% endblock %}

{% block body %}
    <div class="w-full px-6 py-6 mx-auto">
        <div class="flex flex-wrap -mx-3">
            <div class="flex-none w-full max-w-full px-3">
                <div class="relative flex flex-col min-w-0 mb-6 break-words bg-white border-0 border-transparent border-solid shadow-soft-xl rounded-2xl bg-clip-border">
                    <div class="p-6 pb-0 mb-0 bg-white border-b-0 border-b-solid rounded-t-2xl border-b-transparent">
                        <div class="flex flex-wrap -mx-3">
                            <div class="flex items-center w-full max-w-full px-3 shrink-0 md:w-8/12 md:flex-none">
                                <h6 class="mb-0">{{ character.name|raw }}</h6>
                            </div>
                        </div>
                    </div>

                    <!-- Tabs -->
                    <div class="flex flex-wrap px-6 pt-4">
                        <div class="flex-auto">
                            <ul class="flex flex-row flex-wrap list-none border-b-0 pl-0" role="tablist">
                                <li class="flex-auto text-center">
                                    <a href="{{ path('app_character_edit', {'id': character.id}) }}" 
                                       class="block px-6 py-3 mb-0 ml-2 font-bold text-center text-slate-700 uppercase align-middle transition-all ease-in border-b-2 border-transparent border-solid cursor-pointer text-xs bg-transparent leading-pro tracking-tight-soft hover:isolate focus:isolate {% if app.request.attributes.get('_route') == 'app_character_edit' %}border-cyan-500{% endif %}"
                                       role="tab">
                                        <i class="fas fa-pencil-alt mr-2"></i>Fiche personnage
                                    </a>
                                </li>
                                <li class="flex-auto text-center">
                                    <a href="{{ path('app_character_background', {'id': character.id}) }}" 
                                       class="block px-6 py-3 mb-0 ml-2 font-bold text-center text-slate-700 uppercase align-middle transition-all ease-in border-b-2 border-transparent border-solid cursor-pointer text-xs bg-transparent leading-pro tracking-tight-soft hover:isolate focus:isolate {% if app.request.attributes.get('_route') == 'app_character_background' %}border-cyan-500{% endif %}"
                                       role="tab">
                                        <i class="fas fa-file-alt mr-2"></i>Background
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>

                    <div class="flex-auto px-0 pt-0 pb-2">
                        <div class="p-6">
                            <form method="POST" action="{{ path('app_character_background', {'id': character.id}) }}">
                                <!-- Description -->
                                <div class="flex flex-wrap -mx-3 mb-6">
                                    <div class="w-full max-w-full px-3">
                                        <div class="mb-4">
                                            <label for="description" class="inline-block mb-2 ml-1 font-bold text-xs text-slate-700">Description physique</label>
                                            <textarea name="description" rows="4" class="focus:shadow-soft-primary-outline text-sm leading-5.6 ease-soft block w-full appearance-none rounded-lg border border-solid border-gray-300 bg-white bg-clip-padding px-3 py-2 font-normal text-gray-700 outline-none transition-all placeholder:text-gray-500 focus:border-cyan-300 focus:outline-none">{{ character.description }}</textarea>
                                        </div>
                                    </div>
                                </div>

                                <!-- Background -->
                                <div class="flex flex-wrap -mx-3 mb-6">
                                    <div class="w-full max-w-full px-3">
                                        <div class="mb-4">
                                            <label for="background" class="inline-block mb-2 ml-1 font-bold text-xs text-slate-700">Histoire du personnage</label>
                                            <textarea name="background" rows="8" class="focus:shadow-soft-primary-outline text-sm leading-5.6 ease-soft block w-full appearance-none rounded-lg border border-solid border-gray-300 bg-white bg-clip-padding px-3 py-2 font-normal text-gray-700 outline-none transition-all placeholder:text-gray-500 focus:border-cyan-300 focus:outline-none">{{ character.background }}</textarea>
                                        </div>
                                    </div>
                                </div>

                                <!-- Notes des orgas -->
                                <div class="flex flex-wrap -mx-3 mb-6">
                                    <div class="w-full max-w-full px-3">
                                        <div class="mb-4">
                                            <label for="note_orga" class="inline-block mb-2 ml-1 font-bold text-xs text-slate-700">Notes des orgas</label>
                                            <textarea name="note_orga" rows="4" class="focus:shadow-soft-primary-outline text-sm leading-5.6 ease-soft block w-full appearance-none rounded-lg border border-solid border-gray-300 bg-white bg-clip-padding px-3 py-2 font-normal text-gray-700 outline-none transition-all placeholder:text-gray-500 focus:border-cyan-300 focus:outline-none bg-gray-100" readonly>{{ character.noteOrga }}</textarea>
                                        </div>
                                    </div>
                                </div>

                                <div class="w-full p-1 mx-auto text-right">
                                    <button type="submit" class="inline-block px-6 py-3 font-bold text-center text-white uppercase align-middle transition-all rounded-lg cursor-pointer bg-gradient-to-tl from-green-600 to-green-700 leading-pro text-xs ease-soft-in tracking-tight-soft shadow-soft-md bg-150 bg-x-25 hover:scale-102 active:opacity-85 hover:text-lime-300 hover:shadow-soft-xs">Sauvegarder</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %} 